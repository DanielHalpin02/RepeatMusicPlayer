<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RepeatMusicPlayer.MainPage"
             Title="Library">

    <VerticalStackLayout>

        <Button Text="Playlists" Clicked="OnPlaylistsClicked" />

        <CollectionView x:Name="SongsCollectionView"
                         ItemsSource="{Binding Songs}"
                         SelectionMode="Single"
                         SelectionChanged="OnSongSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="10">
                        <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold" />
                        <Label Text="{Binding Artist}" FontSize="14" TextColor="Gray" />
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>

</ContentPage>